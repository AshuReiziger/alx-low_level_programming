#include <stdio.h>
#include "main.h"

/**
 * main - Prints the program name, followed by a new line.
 * @argc: The number of arguments supplied to the program.
 * @argv: An arrat of pointers to the arguments.
 * return: Always 0.
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
